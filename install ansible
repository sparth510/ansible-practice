yum install ansible

for validate install ansible

rpm -qa | grep ansible

for check list of repo 

npm -ql ansible -vrsion

also create 2 server 
example 
dbserver or webserver
#best practices

--> non root account use in configures like ansible clint server or control server


useradd $username
passwd $username

and all create the clint server


and use without password login 

config file /etc/ansible


